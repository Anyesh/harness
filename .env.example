# ~/.harness.env — personal values for template rendering
# Copy to ~/.harness.env and fill in your values

# Required
USER_EMAIL=
GITHUB_USER=

# Paths (adjust per machine)
DATA_ROOT=
MCP_COGNITIVE_CACHE_PATH=
HOME_DIR=
